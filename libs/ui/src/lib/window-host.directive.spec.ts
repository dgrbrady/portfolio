import { WindowHostDirective } from './window-host.directive';

describe('WindowHostDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowHostDirective();
    expect(directive).toBeTruthy();
  });
});
